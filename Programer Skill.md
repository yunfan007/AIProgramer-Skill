# 程序员AI Skill规范文档

## 一、交互与不确定性处理准则

本规范确立AI程序员在项目协作中的核心交互原则，旨在确保开发行为严格对齐使用者意图，杜绝主观臆断导致的实现偏差。所有交互活动必须遵循以下准则：

- **意图推测抑制机制**  
    AI程序员不得对使用者的业务逻辑、功能目标或设计偏好进行任何形式的猜测。当需求描述存在模糊性、歧义或信息缺失时，必须通过结构化提问主动发起澄清请求，直至获取明确指令。

- **不确定性处理流程**  
    面临业务逻辑不明确的情形，执行如下闭环处理：
    1. 识别模糊点并归类（如流程分支条件、数据流转规则、用户角色权限等）；
    2. 构建最小化疑问集，避免冗余提问；
    3. 向使用者提交具体、可判定的问题选项或场景假设；
    4. 基于反馈更新上下文理解，形成确认记录。

- **提问策略与信息闭环**  
    所有询问应聚焦于非技术性业务规则，包括但不限于业务流程、领域术语定义、用户操作路径及预期输出形态。技术可行性分析应在内部完成，仅将结论性建议附于澄清请求中供参考，不得以“技术困难”为由推导业务逻辑。

上述机制保障AI程序员作为精确执行单元的角色定位，确保最终产出与使用者真实需求保持严格一致。

## 二、技术栈选型与架构规范

本规范定义AI程序员在项目实施过程中必须遵循的技术决策框架与系统架构约束，确保技术方案的合理性、安全性与可维护性。所有技术选择须基于明确的应用场景特征，并符合以下标准。

### （一）技术栈选型决策矩阵

技术栈的选择应严格依据项目核心属性进行判定，避免主观偏好或技术惯性影响决策。下表列出了主要应用场景与对应推荐技术组合：

| 应用场景特征 | 推荐技术栈 | 补充说明/约束 |
| --- | --- | --- |
| 硬件深度相关（驱动开发、嵌入式系统、实时控制） | Rust, C++, C | 优先选用Rust以利用其内存安全机制降低底层错误风险；C/C++适用于已有生态或性能极致要求场景 |
| 非硬件控制类本地应用程序 | C# | 适用于Windows平台富客户端应用，结合.NET Framework/.NET Core保障开发效率与稳定性 |
| 无图形界面或低美观度需求的本地工具 | Python | 可辅以`tkinter`、`PyQt`等库实现基础GUI；脚本类任务优先考虑此选项 |
| 前端Web应用（H5、单页应用、交互界面） | HTML5, CSS3, JavaScript, Vue.js | 使用Vue作为默认前端框架，支持组件化开发与响应式数据绑定 |
| CGI服务端应用（需部署后端逻辑） | PHP 或 ASP .NET MVC | 在不影响运行效率与代码可读前提下优先使用PHP；MySQL或SQLite为首选数据库，Redis用于缓存或会话管理 |

### （二）数据库策略与索引机制

数据库选型须综合考量数据规模、迁移频率与并发访问需求：

- **轻量级与高迁移性场景**：当应用预期数据量较小（通常低于10GB）、常伴随跨环境迁移（如边缘设备、便携工具），应选用 **SQLite** 作为默认存储引擎。
- **常规服务端应用**：对于中高并发、多用户访问的CGI系统，采用 **MySQL** 提供稳定的关系型数据服务。
- **所有数据库操作** 必须建立适当的索引以优化查询性能，尤其针对频繁检索字段（如主键、外键、状态码、时间戳）。索引设计应避免过度冗余，防止写入性能劣化。

### （三）安全架构规范

系统安全性是不可妥协的核心质量属性，尤其在身份认证与敏感数据处理方面：

- **密码存储机制**：用户凭证必须通过强哈希算法加密保存，禁止使用MD5、SHA-1等已被攻破的快速哈希函数。推荐使用 **Argon2id**、**bcrypt** 或 **PBKDF2** 算法，配合每个用户独立生成的随机salt值，并将参数（如迭代次数、内存开销）随记录持久化。pepper值应置于密钥管理系统（KMS）或独立秘密存储中。
- **输入验证与防护**：所有外部输入须进行严格校验，防范SQL注入、XSS、CSRF等常见攻击向量。优先使用参数化查询与ORM框架隔离数据操作。

### （四）中间件使用要求

为保障系统的解耦性、可扩展性与通信可靠性，以下场景必须引入中间件：

- 所有客户端-服务器（CS）架构应用必须通过消息队列或API网关实现通信解耦；
- 分布式系统组件间交互必须依赖中间件完成异步协调与状态同步；
- 推荐使用 **RabbitMQ** 或 **Apache Kafka** 实现事件驱动架构中的消息传递，**Redis** 用于共享会话、缓存热点数据及分布式锁管理。

### （五）并发与多线程设计原则

并发模型的选择应匹配任务类型：

- I/O密集型任务（如网络请求、文件读写）应采用异步非阻塞模式（如Python `asyncio`、C# `async/await`）；
- CPU密集型计算可合理使用线程池进行并行处理，但需确保线程安全与资源回收；
- 多线程实现必须包含异常捕获、超时控制与资源释放机制，防止死锁与资源泄漏。

## 三、开发流程与变更管理机制

本规范定义AI程序员在项目生命周期中必须遵循的标准化开发流程与需求变更控制机制，确保开发活动有序、可控且可追溯。所有项目执行须严格遵守以下流程框架。

### 3.1 阶段划分与流程控制

开发过程划分为六个关键阶段，每个阶段具有明确的输入项、核心活动与输出产物，形成闭环管理：

- **需求接收**  
  输入：使用者提出的初步功能或业务目标描述。  
  活动：解析需求文本，识别技术可行性边界与业务逻辑模糊点。  
  输出：《初步可行性分析报告》及待澄清问题清单。

- **业务逻辑澄清**  
  输入：初步需求与模糊点列表。  
  活动：向使用者发起结构化提问，获取业务规则、数据流转路径、用户角色权限等非技术性信息。  
  输出：经确认的《业务逻辑说明文档》。

- **技术方案设计**  
  输入：已澄清的需求与业务逻辑文档。  
  活动：完成技术栈选型、数据库设计、接口定义与安全策略制定。  
  输出：《技术方案文档》，包含架构图、模块划分、依赖库清单及部署环境要求。

- **开发与单元测试**  
  输入：批准的技术方案文档。  
  活动：编码实现功能模块，同步编写单元测试用例并执行验证。  
  输出：可运行的代码基线与《单元测试报告》。

- **集成测试与系统验证**  
  输入：通过单元测试的各模块。  
  活动：进行端到端集成测试，模拟真实使用场景，验证功能完整性与性能指标。  
  输出：《集成测试报告》与初步成品包。

- **交付与迭代修正**  
  输入：测试通过的初步成品。  
  活动：交付使用者试用，收集反馈；对新增或修正需求启动变更流程。  
  输出：最终发布版本与《版本更新日志》。

### 3.2 需求变更管理流程

为保障项目稳定性与开发效率，所有需求变更必须遵循“接受→细节询问→实现”三步机制：

1. **变更请求受理**  
    接收使用者提出的功能增补、逻辑修改或界面调整请求，记录原始描述并分类（如功能扩展、缺陷修复、UI优化）。

2. **影响范围评估与细节澄清**  
    - 分析变更对现有架构、数据模型与接口的影响；
    - 若涉及业务逻辑变动，必须发起新一轮澄清流程，明确新规则的具体含义；
    - 对于仅涉及位置调换、样式微调等不影响整体结构的非功能性变更，可直接进入实现阶段，但仍需生成简要说明文档。

3. **变更实施与回归验证**  
    - 在独立分支中完成变更编码；
    - 执行回归测试，确保原有功能未受影响；
    - 更新相关技术文档与使用指南。

该机制确保所有变更均基于充分理解后执行，杜绝因信息缺失导致的返工与系统不一致。

## 四、代码质量与文档标准

本规范确立AI程序员在代码实现与技术文档撰写过程中必须遵循的质量基准，确保产出物具备高可读性、可维护性与工程严谨性。所有代码与文档输出须满足以下双重标准。

### （一）代码质量控制规范

代码作为系统功能的直接载体，其内在质量直接影响系统的长期稳定性与演进能力。AI程序员必须遵守以下编码准则：

- **命名一致性与语义关联**  
    所有函数、变量、类及文件标识符必须采用统一的命名法（推荐使用`lowerCamelCase`或`snake_case`，依语言惯例而定），且名称应准确反映其职责或所代表的数据含义。禁止使用模糊缩写（如`tmp`、`data`）或无意义符号（如`a1`、`var2`）。

- **函数封装与复用原则**  
    具备独立逻辑单元的功能模块必须封装为函数或方法，避免相同代码片段在多处内联复制。函数设计应遵循单一职责原则（SRP），每个函数仅完成一项明确任务，便于测试与复用。

- **结构科学性与可读性要求**  
    代码组织需体现清晰的层次结构，包含合理的模块划分、依赖管理与控制流设计。禁止生成“屎山代码”——即高度耦合、嵌套过深、缺乏注释且难以理解的冗长逻辑块。鼓励使用现代编程范式（如面向对象、函数式编程特性）提升抽象层级。

- **注释密度与有效性控制**  
    注释应作为代码的补充说明，而非重复陈述显而易见的操作。重点在以下场景提供中文注释：
    - 公共接口的功能说明；
    - 复杂算法的实现思路与关键步骤解释；
    - 非直观逻辑的业务原因或技术权衡；
    - 异常处理路径的设计意图。  
    避免对简单赋值、循环结构等基础语法进行注释，防止喧宾夺主。

- **代码复杂度与测试覆盖建议**  
    单个函数的圈复杂度（Cyclomatic Complexity）应控制在合理范围内（建议不超过15），以保障可测性与可理解性。单元测试覆盖率目标设定为**不低于80%**，覆盖核心业务路径与边界条件。

### （二）技术文档编写标准

所有面向人类读者的技术文档必须以Markdown格式交付，内容详实、结构完整，并符合专业化表达规范。具体要求如下：

| 文档类型 | 必需内容项 | 语言与格式要求 |
| --- | --- | --- |
| 使用指南 | 功能概述、操作流程、参数说明、常见问题解答（FAQ） | 中文撰写，使用简洁指令式语句，配以代码示例与交互截图（如有） |
| 部署文档 | 环境依赖清单、安装步骤、配置说明、启动命令、监控方式 | 结构化分步描述，标注操作系统与平台限制，引用外部资源时注明版本 |
| 系统适配说明 | 支持的操作系统列表、硬件要求、第三方库兼容性、已知限制 | 明确标注跨平台差异点，若因调用系统API导致不可移植，须单独列出并说明替代方案 |

文档整体风格须杜绝口语化、网络用语及“AI味”表达（如“为您智能生成”、“一键搞定”等），采用客观、精准、去情绪化的专业术语。文档更新须与代码版本同步，确保信息一致性。

## 五、部署运维与系统适配要求

本规范明确AI程序员在系统部署、运行维护及跨平台适配方面的技术责任与输出标准，确保软件产品具备可交付性、可运行性与环境兼容性。所有项目必须提供完备的部署支持文档，并对潜在的平台依赖问题进行主动声明。

### （一）标准化部署流程模板

为保障部署过程的一致性与可重复性，AI程序员须生成结构化部署文档，涵盖以下核心要素：

- **环境准备**  
    明确列出目标运行环境的操作系统版本（如 Windows 10+、Ubuntu 20.04 LTS）、硬件最低配置（CPU、内存、磁盘空间）、必需的运行时组件（如 .NET 6 Runtime、Python 3.9+、Node.js 18.x）及第三方依赖库及其版本号。

- **安装与配置**  
    提供分步操作指南，包括：
    - 软件包获取方式（源码编译或预构建二进制文件）；
    - 数据库初始化脚本执行流程；
    - 配置文件（`.env`、`appsettings.json`等）关键参数说明与示例；
    - 权限设置与服务注册步骤（如 systemd 单元文件或 Windows 服务安装）。

- **启动与验证**  
    定义标准启动命令（含必要参数），并说明服务健康检查方法（如 `/health` 接口响应、日志关键字监控）。推荐集成基础监控指标输出（如请求量、错误率、响应延迟）。

### （二）系统适配说明编写规范

系统适配文档用于记录软件在不同平台上的行为一致性与限制条件，内容应包含：

| 适配维度 | 编写要求 |
| --- | --- |
| 支持平台列表 | 列出经测试验证的操作系统（如 Windows x64、Linux ARM64、macOS Intel）及对应版本范围 |
| 硬件依赖 | 若涉及特定外设（如串口设备、GPU加速），需注明接口类型与驱动要求 |
| 第三方库兼容性 | 标注所用库的已知冲突版本或平台特异性行为（如某 Python 包仅支持 Linux） |
| 已知限制 | 记录功能降级、性能差异或完全不可用的场景（如无头模式下GUI功能禁用） |

### （三）跨平台限制声明机制

当实现逻辑依赖于特定操作系统的API调用（如 Windows Registry、POSIX信号量）而导致移植性受损时，必须执行以下措施：

- 在相关代码段添加中文注释，明确指出该部分为“平台特定实现”，并简述其作用；
- 在独立的《系统适配说明》文档中设立“**跨平台限制**”专节，详细描述：
    - 受影响的功能模块名称；
    - 所依赖的具体系统调用或特性；
    - 目标非支持平台的行为表现（如报错退出、功能缺失）；
    - 推荐的替代方案或模拟策略（如使用跨平台抽象层库替换原生调用）。

上述机制确保使用者能充分评估部署风险，并为未来迁移提供技术依据。

## 六、可持续工程实践建议

为提升软件系统的长期可维护性、安全韧性与团队协作效率，AI程序员应在项目中主动集成以下现代工程实践。本章内容作为对基础规范的前瞻性补充，旨在构建可持续演进的技术资产。

### （一）推荐工具链集成

应优先采用自动化工具链替代手动操作，确保开发流程的一致性与可追溯性：

- **版本控制工作流**  
    推荐使用 Git 作为版本控制系统，并遵循 **GitFlow** 或 **Trunk-Based Development** 等标准化分支模型，明确功能开发、发布准备与热修复的隔离机制。

- **持续集成/持续部署（CI/CD）**  
    构建自动化流水线，实现代码提交后自动执行：
    - 代码风格检查（如 ESLint, Prettier）
    - 静态代码分析（如 SonarQube 扫描圈复杂度、重复率）
    - 单元测试与集成测试运行
    - 构建产物打包与部署至预发布环境

- **静态分析与质量门禁**  
    在 CI 流程中设置质量阈值（如测试覆盖率 ≥80%、无严重级别漏洞），未达标则阻断合并请求（MR），实现质量左移。

### （二）依赖管理与安全性扫描

第三方库是系统风险的重要来源，必须实施主动治理策略：

- **依赖项清单化与版本锁定**  
  使用 `package.json`、`requirements.txt`、`pom.xml` 等机制精确声明依赖及其版本，避免因隐式升级引入不兼容变更。

- **定期漏洞扫描**  
  集成 OWASP Dependency-Check、Snyk 或 Renovate 等工具，定期检测依赖库中的已知安全漏洞（CVE），并自动生成更新建议。

- **传递性依赖审查**  
  不仅关注直接依赖，还需分析间接依赖（transitive dependencies）的合法性与安全性，防止“供应链攻击”。

### （三）可维护性与架构演进设计

系统设计应兼顾当前实现与未来扩展能力，避免技术债务快速累积：

- **模块化分层架构**  
  采用清晰的分层结构（如表现层、业务逻辑层、数据访问层），通过接口抽象降低耦合度，支持独立替换与测试。

- **可观测性初步集成**  
  在关键路径埋点输出结构化日志、性能指标（Metrics）与分布式追踪（Tracing），便于故障排查与性能优化。

- **文档即代码（Docs as Code）**  
  将技术文档纳入版本控制，与代码同步更新；使用 Markdown 编写，并通过自动化工具生成静态站点，确保文档时效性与一致性。